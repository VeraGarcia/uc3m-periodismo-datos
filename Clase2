Periodismo guiado por datos (un periodista que emplea datos)  cercano al periodismo de investigación. En 2008 emerge con los movimientos de datos y en España con 2014-15 con la Ley de Transparencia.  Gran  dispolnibilidad de la administración pública de datos. 
1989-1990  -  Las bases de datos estructurados  se ponen en  disposición. Se empieza a conocer como perriodismo de base de datos. 
CSV - Valores separados por comas. En los ordenadores, Filter tau. Mark down es una sintasis simple a la vez que es un progama qu transforma esa sintasis en una sintasis estructurada. STRINGS significa cadena de caracteres. TODO el libro se puede consideraar un tipo de dato (STRING)que alude al lenguaje natural. Al principio, lo que se utilizaba para separar los valores era TAB. Esta tecla de tabulación hace referencia al TSV (Valores separados por tabulación), CSV o en general *SV (EL * es comodín para hablar de cualquier dato separado po cualquier carácter). 
SQL - Intimamente relacionado con las BBDD (Bases de datos).
La diferencia entre TSV, *sV Y CSV es la forma de separar los datos. 
Tipos de formato de datos fundamentalmente. JSON, *SCV Y JSON. Lenguaje que va a estructurar datos. XML es un puente entre el mundo de datos y texto. Es util para hacer una replica de bases de datos. En el mundo informático, hay mil maneras de llegar al mismo punto. Los JSON son también en modo texto. JS (Utiliza la sintaxis de Java Script) y Notación de objetos. Para aplicaciones web, es lo que más se utiliza porque es el lenguaje nativo de la web. 
Hay tipos de datos. Tabla porque tiene forma de tabla pero si se "Guarda como" se guardaría como CSV. Todas las tablas suelen que no siempre tener el nº de lectura de izquierda a derecha, filas y columnas o lo que es lo mismo rows y columns. La primera fila suele ser la que indica qué tipo de información hay (NIU, números).  Uno de los tipos de datos que hay son números. La segunda columna representa los apellidos. Esta simple tabla tiene una gran complejidad. Este dato (2021-09-21) tiene formato fecha. Las fechas son números pero no con los que hacer operaciones. YYYY-MM-DD es la nomenclatura estándar para trabajar con fechas. El CSV no es un estándar que haya surgido de una institución académica sino su propio uso. Según el tipo de fecha te puede interesar saber sólo la medición del día o la de la hora (YYYY-MM-DDT 12:43). Datos BOOLEANOS - 0/1, t/f, S/N. Posibilidad u otra. 
"OpenRefine" - La navaja suiza del periodismo de datos. Es para trabajar con datos sucios. Dato que está manchando la columna. El pograma funciona a traves de la máquina virtual JAVA Y lanza una aplicación web. Como el CSV es un formato poco estandarizado, la codificación de carácteres les afecta lo que está relacionado con la informatica. La codificación de carácteres es como el teclado. No contemplan la A con tilde, por ejemplo. Su codificación más usada en ese tipo de archivos es ASCII es un estándar sin las tildes, sin la Ñ. Eso evolucionará a los ISO-8850 O ISO-1,15 que tienen ya tildes o las Ñ y ahora existe el UTF-8 tambien llamado UNICODE (la codificación universal con emoticonos, jeroglíficos...). Esto afecta la codificación de los archivos. 
Un formato de datos numericos, aunque son carácteres más que números pues no vamos a hacer operaciones (son identificadores), nos guía. Función JOIN () nos permite unir dos columnas consiguiendo otra columna con el nombre completo. 
TODO está en mayúsculas. Upprcase () para poner todas las letras en mayúscula y Lowrcase () para poner todas las letras en minúscula. 
La dirección de correo repite el número de identificador. 
El tipo de datos se llama "STRINGS", cadena de carácteres. 
